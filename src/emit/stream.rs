//! Rust-source emitter for a future `vm::stream` program -- one of
//! `emit`'s three emitters (see module docs).
//!
//! **Not yet implemented.** Blocked on [`crate::codegen::stream`] and
//! [`crate::vm::stream`] existing for real. [`super::batch`] is the
//! reference for what "done" looks like here.
