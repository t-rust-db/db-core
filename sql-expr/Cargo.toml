[package]
name = "sql-expr"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "SQL expression and query AST types (no evaluation, no parsing)"

[dependencies]
sql-types = { path = "../sql-types" }
