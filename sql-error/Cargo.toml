[package]
name = "sql-error"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Shared source-location primitive for t-rust-db error types (no central Error type, no external error crates)"

[dependencies]
